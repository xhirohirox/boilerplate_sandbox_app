= simple_form_for(:recruitment, method: :post, url: company_users_recruitments_path) do |f|
  .field
    = f.label "求人名"
    = f.text_field :name
    = f.label "詳細"
    = f.text_field :description
  .actions
    = f.submit 'Save'
